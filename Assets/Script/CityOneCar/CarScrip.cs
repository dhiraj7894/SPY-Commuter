using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarScrip : MonoBehaviour
{

    // Update is called once per frame
/*    void Update()
    {
        transform.position = Vector3.MoveTowards(SpwanScripManager.instance.startPosition.position, SpwanScripManager.instance.EndPosition.position, GameManager.instance.carSpeed * Time.deltaTime); ;
        if(transform.position.z >= 26)
        {
            transform.position = SpwanScripManager.instance.EndPosition.position;
        }
    }*/
}
